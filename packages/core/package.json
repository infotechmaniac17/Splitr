{
  "name": "@splitr/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types, zod schemas, API client, and money formatting for Splitr web + mobile. Mirrors backend/app/api/schemas.py (OpenAPI contract) — do not hand-write duplicate types in web/ or mobile/.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
