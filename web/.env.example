# Base URL of the Splitr FastAPI backend (backend/app), no trailing slash.
# Must be NEXT_PUBLIC_* because client components (upload polling, assignment
# screen) call the API directly from the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
